# 🚲 Пункт Велопроката 

## Техническое задание
Разработана система управления пунктом проката велосипедов на основе ASP.NET Core 8.0 с использованием MongoDB в качестве базы данных. Реализовано REST API с полным набором CRUD операций для управления сущностями: велосипеды, модели велосипедов, арендаторы и записи об аренде.
### База данных пункта проката хранит:
1. **Велосипеды** - серийный номер, модель, цвет
2. **Модели велосипедов**:
   - Тип велосипеда 
   - Размер колес
   - Предельно допустимый вес пассажира
   - Вес велосипеда
   - Тип тормозов
   - Модельный год
   - Цена часа аренды
3. **Арендаторы** - ФИО, телефон
4. **Аренды** - время начала, продолжительность

### Юнит-тесты:
1. Вывести информацию обо всех спортивных велосипедах
2. Вывести топ 5 моделей велосипедов (по прибыли от аренды и по длительности аренды отдельно).
3. Вывести информацию о минимальном, максимальном и среднем времени аренды велосипедов.
4. Вывести суммарное время аренды велосипедов каждого типа.
5. Вывести информацию о клиентах, бравших велосипеды на прокат больше всего раз.


В рамках расширения функциональности реализована подсистема генерации и обработки тестовых данных:

Генератор контрактов - отдельное приложение, которое создает реалистичные тестовые данные аренды с использованием библиотеки Bogus и отправляет их батчами в NATS JetStream для асинхронной обработки.

Потребитель сообщений - на стороне основного API реализован фоновый сервис (BackgroundService), который подключается к NATS JetStream, создает stream и consumer для обработки входящих сообщений. Каждое полученное сообщение автоматически десериализуется в список DTO и сохраняется в MongoDB через бизнес-сервис аренды.

Оркестрация через Aspire - система конфигурации включает инфраструктуру NATS с JetStream поддержкой и мониторингом на порту 8222. Централизованно настроены все параметры подключения: учетные данные NATS, имена stream и subject, которые автоматически передаются как в генератор, так и в API приложение.

### Технологический стек
1. Backend: ASP.NET Core 8.0, C# 12
2. База данных: MongoDB
3. Брокер сообщений: NATS с JetStream
4. Генерация данных: Bogus
4. Оркестрация: .NET Aspire
5. Тестирование: xUnit
